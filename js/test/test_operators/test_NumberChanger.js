const expect = require('chai').expect;
const should = require('chai').should();

const op = require('../../bin/src/operators/NumberChangerOperator');
const msg = require('../message');


describe(msg.message(op.NumberChangerOperator), function () {

});