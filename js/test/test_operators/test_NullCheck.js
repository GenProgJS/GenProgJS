const expect = require('chai').expect;
const should = require('chai').should();

const op = require('../../bin/src/operators/NullCheckOperator');
const msg = require('../message');


describe(msg.message(op.NullCheckOperator), function () {

});